[TASK: NEXT QUESTION — REASON FIRST, THEN ASK ONE]

너는 지금 이 사건의 과실비율을 판정하기 위해 무엇을 더 알아야 하는지 스스로 판단해야 한다.
미리 정해진 질문 목록은 없다. 사건 상태와 영상 분석 결과를 읽고 네가 직접 추론하라.

<CASE_STATE>
{{case_state}}
</CASE_STATE>

영상 분석이 확인하지 못했거나 추정만 한 항목:
{{video_unknowns}}

영상 분석 요약:
{{video_summary}}

반드시 먼저 물어야 하는 항목(있으면 이 항목을 첫 질문으로 한다):
{{forced_field}}

이미 질문했거나 확인된 항목과 그 답(같은 내용을 다른 이름·표현으로 다시 묻지 않는다):
{{asked_fields}}

추론 순서 (reasoning에 그대로 적는다):
1. 이 사고의 구조(장소 유형, 양 차량의 진행 관계, 충돌 형태)를 한 줄로 정리한다.
2. 이 구조에서 기본과실을 정하거나 수정요소로 작용하는 요소들을 네 지식으로 열거한다.
   (예: 교차로 선진입, 신호, 방향지시등, 실선/점선, 서행·일시정지, 도로 폭, 영상 시작 전 상황, 별도 충돌 …) — 이 예시에 얽매이지 말고 이 사건에 맞게 생각하라.
3. 그중 영상에서 CONFIRMED 되지 않은 것을 고른다.
4. 그중 사용자가 객관적으로 답할 수 있는 것(직접 보았거나 알 수 있는 사실)을 고른다. 영상으로 다시 확인 가능한 것은 제외한다.
5. 답에 따라 비율이 가장 크게 달라지는 것 하나를 선택한다. 판정과 무관한 정보(사고 일시, 장소명)는 지금 묻지 않는다.
6. 더 물어볼 가치가 있는 것이 없으면 questions를 빈 배열로 두고 reasoning에 그 이유를 적는다.

금지:
- 사용자의 주관적 과실 판단("잘못", "무리하게", "생각하시나요")을 묻는 것
- 이미 확인되었거나 이미 물어본 항목을 다시 묻는 것
- 한 번에 두 개 이상 묻는 것 (최대 {{max_questions}}개)

작성 규칙:
- field: Case State에 맞는 슬롯 경로가 있으면 그것(예: other_vehicle.turn_signal, ego_vehicle.entered_first, road.signal_state,
  video_source.vehicle_owner), 없으면 `review.<snake_case>`로 네가 이름을 만든다 (예: review.solid_line_lane_change, review.pre_video_situation).
- question: 한 문장, 존댓말, 선택지를 제시할 수 있으면 제시. 영상에서 식별된 vehicle ID와 설명으로 어떤 차량인지 명확히 지칭.
- why: 이 답이 어떤 과실 요소(기본과실 유형 또는 수정요소)의 적용을 결정하는지 한 문장. 사용자에게 그대로 보여준다.
- intro: 첫 대화 턴이면 영상에서 확인된 내용을 1~2문장으로 요약한 뒤 질문으로 이어지는 안내문. 이미 대화 중이면 빈 문자열.

출력 JSON:
{
  "reasoning": [
    "사고 구조: 비신호 2차로 회전교차로에서 내측 차로 차량이 외측 차로로 진로 변경 중 측면 접촉",
    "판정 요소: 진로 변경 차량의 방향지시등, 회전 중인 차량의 선진입 여부, 실선/점선 구간, 서행 여부",
    "영상 미확인: 방향지시등(화각 밖), 실선 여부(노면 불명확) / 선진입은 영상에서 CONFIRMED",
    "사용자가 알 수 있는 것: 방향지시등(직접 봤을 수 있음), 실선 여부",
    "비율 영향이 가장 큰 것: 방향지시등 미점등 수정요소"
  ],
  "intro": "",
  "questions": [
    {"field": "other_vehicle.turn_signal", "question": "상대 차량(vehicle_2)이 차로를 바꿀 때 방향지시등을 켠 것을 보셨나요?", "importance": "high", "why": "방향지시등 미점등은 진로 변경 차량 과실을 가산하는 수정요소입니다."}
  ]
}
